#include <iostream>
using namespace std;

int main()
{
    int a,b;
    cin >> a;
    cin >> b;
    a == b ? cout << "WORLD WAR 3!" << endl : (a > b ? cout << "MAGA!" << endl : cout << "FAKE NEWS!" << endl);
}
