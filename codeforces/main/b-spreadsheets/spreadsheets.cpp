#include <iostream>

using namespace std;

int main()
{
	int n;
	for (int i = 0; i <= n; i++)
	{
		string c;
		cin >> c;

		if (c[0] == 'R')
		{
			size_t i = 1;
			while (i < c.size() && isdigit(s[i])) ++i;
			
			if (i > 1) {
        			return std::stoi(s.substr(1, i-1));
    			}
		}
		else
		{
			
		}
	}
}
